<%= simple_form_for @promotion do |f| %>
	<%= f.input :title, label: false, placeholder: 'Title' %>
	<%= f.input :description, :input_html => { :rows => 2}, label: false, placeholder: 'Enter description' %>
	<%= f.submit %>
	<% end %>
